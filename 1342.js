/**
 * @param {number} num
 * @return {number}
 */
var numberOfSteps = function (num) {
  // let times = 0;
  // while (num !== 0) {
  //   times++;
  //   num = num % 2 === 0 ? num / 2 : num - 1;
  // }
  // return times;
  console.log(num.toString(2));
};

numberOfSteps(0);
